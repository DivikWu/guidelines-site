---
title: 浅色与深色模式
description: 浅色/深色主题完整色值表、背景层级、语义变量与首帧说明，与当前实现一致
category: 基础规范
status: Draft
last_updated: 2026-02-01
---

# 🌓 浅色与深色模式

本文档整理 YAMI 设计系统当前**浅色模式**与**深色模式**的完整色值、背景层级、语义变量及首帧行为，供设计与开发查阅。所有数值以代码实现为准；若与历史报告（如 docs/ 下 DARK_MODE_*.md）不一致，以本文为准。

---

## 文档说明

### 数据来源

- **Token 源**：[tokens/tokens.json](tokens/tokens.json)，经 `scripts/generate-tokens.py` 生成 [styles/tokens.css](styles/tokens.css)。
- **首帧与 meta**：[app/layout.tsx](app/layout.tsx) 内联脚本：设置 `data-theme`、`color-scheme`、`meta[name="theme-color"]`，深色时注入 `#critical-dark-vars` 内联样式以避免首屏闪烁。
- **语义变量**：[app/globals.css](app/globals.css) 中 `:root`（浅色）与 `[data-theme='dark']`（深色）定义 `--foreground-*`、`--background-*`、`--state-*` 等；[styles/theme.css](styles/theme.css) 提供基础 Token→语义映射。

### 约定

- 组件样式**优先使用语义变量**（如 `--foreground-primary`、`--background-secondary`、`--state-hover`），不硬编码 hex/rgba。
- 新增或修改色值时仅改 `tokens.json`，运行 `generate-tokens.py`；若 [app/layout.tsx](app/layout.tsx) 首帧脚本中有重复写死的值（如 theme-color、`--background-hero`），需同步更新。

---

## 浅色模式

### 背景层级

浅色模式下页面主背景与表面均为白色，无单独 page/hero 区分：

- **主背景 / 表面**：`background.light` = `surface.light` = `#FFFFFF`，对应 `--background-primary`、`--background-secondary`。

### 色值表（浅色）

| 分类 | Token | 色值 | 用途 / 语义变量 |
|------|-------|------|------------------|
| **背景** | background.light | `#FFFFFF` | 页面主背景 → `--background-primary` |
| | surface.light | `#FFFFFF` | 卡片、面板 → `--background-secondary` |
| **文字** | text.primary.light | `#1A1A1A` | 主要文字 → `--foreground-primary` |
| | text.secondary.light | `#666666` | 次要文字 → `--foreground-secondary` |
| | text.tertiary.light | `#999999` | 辅助文字 → `--foreground-tertiary` |
| **图标** | icon.primary.light | `#1A1A1A` | 选中态/关键入口 |
| | icon.secondary.light | `#666666` | 默认图标 |
| | icon.muted.light | `#999999` | 装饰/弱信息 |
| **边框** | border.subtle.light | `#EBEBEB` | 低存在感边界 → `--border-subtle` |
| | border.strong.light | `#E5E5E5` | 结构性边界 |
| | border.light | `#E5E5E5` | 通用边框（兼容）→ `--border-normal` |
| **分割线** | divider.section.light | `#EBEBEB` | 章节分割线 → `--divider-section` |
| **代码块** | code.bg.light | `#F5F5F5` | 代码背景 → `--code-bg` |
| | code.text.light | `#1A1A1A` | 代码文字 → `--code-text` |
| | code.border.light | `#EBEBEB` | 代码边框 → `--code-border` |
| **交互态** | state.hover.light | `rgba(0,0,0,0.04)` | 悬停 → `--state-hover` |
| | state.selected.light | `rgba(0,0,0,0.04)` | 选中 → `--state-selected` |
| | state.pressed.light | `rgba(0,0,0,0.12)` | 按下 → `--state-pressed` |
| | state.focus.light | `rgba(0,0,0,0.16)` | 焦点 → `--state-focus` |
| **遮罩** | overlay.light | `rgba(0,0,0,0.45)` | 弹窗/抽屉遮罩（向后兼容） |

---

## 深色模式

### 背景层级

深色模式采用「页面主背景 &lt; Hero &lt; 表面」的层级，便于区分区块：

- **页面主背景**：`background.page.dark` = `#0A0A0A`，用于 body 和根容器 → `--background-primary`、`--background-page`。
- **Hero 区**：`background.hero.dark` = `#0E0E0E`，介于主背景与表面之间 → `--background-hero`。
- **表面**：`surface.dark` = `#141414`，卡片、面板等 → `--background-secondary`。
- **兼容用整体深色背景**：`background.dark` = `#141414`，与表面一致，用于 NavDrawer 等组件。

层级关系：主背景 (10) &lt; Hero (14) &lt; 表面 (20)。

### 色值表（深色）

| 分类 | Token | 色值 | 用途 / 语义变量 |
|------|-------|------|------------------|
| **背景** | background.page.dark | `#0A0A0A` | 页面主背景 → `--background-primary` |
| | background.hero.dark | `#0E0E0E` | Hero 区 → `--background-hero` |
| | surface.dark | `#141414` | 表面 → `--background-secondary` |
| | background.dark | `#141414` | 抽屉等兼容 → 同 surface |
| **文字** | text.primary.dark | `#E5E5E5` | 主要文字 → `--foreground-primary` |
| | text.secondary.dark | `#CCCCCC` | 次要文字 → `--foreground-secondary` |
| | text.tertiary.dark | `#B3B3B3` | 辅助文字 → `--foreground-tertiary` |
| **图标** | icon.primary.dark | `#FFFFFF` | 选中态/关键入口 |
| | icon.secondary.dark | `#CCCCCC` | 默认图标 |
| | icon.muted.dark | `#B3B3B3` | 装饰/弱信息 |
| **边框** | border.subtle.dark | `#2A2A2A` | 装饰性边界（实色 Token） |
| | border.dark | `#262626` | 通用边框（兼容）→ 部分场景 `--color-border` |
| | overlay.onDark.08 | `rgba(255,255,255,0.08)` | 低存在感 → `--border-subtle`（globals 深色） |
| | overlay.onDark.12 | `rgba(255,255,255,0.12)` | 结构性 → `--border-normal`（globals 深色） |
| **分割线** | divider.section.dark | `#2A2A2A` | 实色 Token |
| | overlay.onDark.08 | `rgba(255,255,255,0.08)` | → `--divider-section`（globals 深色） |
| | overlay.onDark.12 | `rgba(255,255,255,0.12)` | → `--divider-normal`（globals 深色） |
| **代码块** | code.bg.dark | `#1A1A1A` | 略亮于表面 → `--code-bg` |
| | code.text.dark | `#E5E5E5` | → `--code-text` |
| | code.border.dark | `#262626` | 略暗于代码背景 → `--code-border` |
| **交互态** | state.hover.dark | `rgba(255,255,255,0.04)` | → `--state-hover` |
| | state.selected.dark | `rgba(255,255,255,0.08)` | → `--state-selected` |
| | state.pressed.dark | `rgba(255,255,255,0.12)` | → `--state-pressed` |
| | state.focus.dark | `rgba(255,255,255,0.24)` | → `--state-focus` |
| **填充/遮罩** | overlay.onDark.06 | `rgba(255,255,255,0.06)` | fill-secondary、palette 等 → `--fill-secondary` |
| | overlay.dark | `rgba(0,0,0,0.65)` | 弹窗遮罩（向后兼容） |

说明：深色下边框与分割线在 globals.css 中映射为 **overlay.onDark** 系列（透明白叠加），以减轻「切碎感」；实色 border.dark / divider.section.dark 仍存在于 Token 中，供需要实线的场景使用。

---

## 语义变量与 Token 对应

以下以 [app/globals.css](app/globals.css) 为准；兼容别名 `--color-*` 仅列出主要项。

| 语义变量 | 浅色 Token / 变量 | 深色 Token / 变量 |
|----------|-------------------|-------------------|
| `--foreground-primary` | text.primary.light `#1A1A1A` | text.primary.dark `#E5E5E5` |
| `--foreground-secondary` | text.secondary.light `#666666` | text.secondary.dark `#CCCCCC` |
| `--foreground-tertiary` | text.tertiary.light `#999999` | text.tertiary.dark `#B3B3B3` |
| `--background-primary` | background.light `#FFFFFF` | background.page.dark `#0A0A0A` |
| `--background-secondary` | surface.light `#FFFFFF` | surface.dark `#141414` |
| `--background-page` | （同 primary） | background.page.dark `#0A0A0A` |
| `--background-hero` | （浅色无单独 hero） | background.hero.dark `#0E0E0E` |
| `--border-subtle` | border.subtle.light `#EBEBEB` | overlay.onDark.08 |
| `--border-normal` | border.light `#E5E5E5` | overlay.onDark.12 |
| `--divider-normal` | border.light | overlay.onDark.12 |
| `--divider-section` | divider.section.light `#EBEBEB` | overlay.onDark.08 |
| `--fill-secondary` | palette.neutral-100 | overlay.onDark.06 |
| `--state-hover` | state.hover.light | state.hover.dark |
| `--state-selected` | state.selected.light | state.selected.dark |
| `--state-pressed` | state.pressed.light | state.pressed.dark |
| `--state-focus` | state.focus.light | state.focus.dark |
| `--code-bg` | code.bg.light `#F5F5F5` | code.bg.dark `#1A1A1A` |
| `--code-text` | code.text.light `#1A1A1A` | code.text.dark `#E5E5E5` |
| `--code-border` | code.border.light `#EBEBEB` | code.border.dark `#262626` |
| `--color-border`（别名） | `--border-normal` | yami-color-border-dark `#262626` |

---

## 首帧与 meta

### meta theme-color

- **浅色**：`#ffffff`
- **深色**：`#0A0A0A`（与 `--background-primary` 一致，避免浏览器外壳与内容区色差）

由 [app/layout.tsx](app/layout.tsx) 内联脚本在首帧根据 `localStorage.getItem("yami-theme")` 设置。

### 深色首帧内联样式（critical-dark-vars）

当主题为 dark 时，脚本向 `<head>` 注入 `<style id="critical-dark-vars">`，在主 CSS 加载前即应用关键深色变量，避免首屏白闪。内联样式中的变量与 globals.css 中 `[data-theme='dark']` 保持一致，例如：

- `--foreground-primary:#E5E5E5`，`--foreground-secondary:#CCCCCC`，`--foreground-tertiary:#B3B3B3`
- `--background-page:#0A0A0A`，`--background-hero:#0E0E0E`，`--background-primary:#0A0A0A`，`--background-secondary:#141414`
- `--fill-secondary:rgba(255,255,255,0.06)`，`--border-subtle:rgba(255,255,255,0.08)`，`--border-normal:rgba(255,255,255,0.12)`，`--divider-*` 同上
- `--state-hover/selected/pressed/focus` 与 Token 一致
- `--color-text-primary`、`--color-surface`、`--color-bg`、`--color-border:#262626` 等兼容别名

若日后从 tokens 生成首帧脚本，可消除此处与 tokens.json 的重复维护。

---

## 文档内容区（深色）

- **表面统一**：深色下文档主内容区（文章容器 `.doc`、表格容器 `.doc-table-wrap`、callout `.doc-callout`）均基于同一表面：文章与表格容器使用 `--background-secondary`（#141414）；表格表头与 callout 使用 `--fill-secondary`（6% 白叠加）作为轻量块，与表面层级一致。
- **表格行悬停**：深色下表格行悬停使用 `--state-selected`（8% 白），以提升可辨度；其余列表/卡片等仍可使用 `--state-hover`（4% 白）。

---

## 使用建议

1. **组件样式**：统一使用语义变量（`--foreground-*`、`--background-*`、`--state-*`、`--code-*` 等），不写死 hex/rgba。
2. **改色流程**：只修改 [tokens/tokens.json](tokens/tokens.json)，运行 `python3 scripts/generate-tokens.py`；若 [app/layout.tsx](app/layout.tsx) 中 theme-color 或 critical-dark-vars 有写死值，需人工同步。
3. **对比度**：主文与主背景在浅/深色下均需满足 WCAG AA（正文 ≥ 4.5:1，大文本 ≥ 3:1）；当前主文 `#E5E5E5` 在 `#0A0A0A` 上已远超该要求。
