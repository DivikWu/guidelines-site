---
title: 圆角
title_en: Corner Radius
description: 圆角层级、嵌套规则与场景应用规范
category: 基础规范
status: Draft
last_updated: 2026-01-31
---

# ⭕ 圆角

圆角赋予界面元素柔和与现代感，是品牌视觉语言的重要组成部分。YAMI 设计系统定义了统一的圆角 Token，确保各组件之间的视觉协调。

---

## 圆角 Token

设计系统提供以下圆角层级：

| Token | 数值 | 用途说明 |
|-------|------|----------|
| radius/none | 0px | 直角，无圆角 |
| radius/small | 4px | 小圆角，用于小型元素（标签、徽标） |
| radius/medium | 8px | 中圆角，用于按钮、输入框等标准组件 |
| radius/large | 12px | 大圆角，用于卡片、弹窗、容器 |
| radius/full | 999px | 全圆角，用于圆形按钮、头像、药丸形状 |

> [!tip] CSS 变量引用
> 在代码中通过 `var(--radius/small)` 等方式引用，确保全局一致性。

---

## 使用场景

### 小型元素

- 标签（Tag）
- 徽标（Badge）
- 小型按钮

→ 使用 **small** 圆角

### 标准组件

- 按钮（Button）
- 输入框（Input）
- 下拉菜单（Dropdown）
- Toast 提示

→ 使用 **medium** 圆角

### 容器与卡片

- 卡片（Card）
- 弹窗（Modal / Dialog）
- 面板（Panel）
- 图片容器

→ 使用 **large** 圆角

### 特殊形状

- 头像（Avatar）
- 圆形按钮（Icon Button）
- 药丸形标签

→ 使用 **full** 圆角

---

## 嵌套圆角

当元素嵌套时，内层圆角应小于外层圆角，以保持视觉平衡。

> [!tip] 设计建议
> 内层圆角 = 外层圆角 - 内边距

例如：卡片使用 large 圆角，卡片内的按钮使用 medium 圆角。

---

## 使用原则

1. **一致性**：同类组件使用相同的圆角层级
2. **层级感**：外层容器圆角 ≥ 内层元素圆角
3. **避免混用**：同一视图中避免出现过多不同的圆角值
4. **平台适配**：iOS 倾向使用较大圆角，Android 可稍小

---

> [!note] Token 来源
> 本文档中的 Token 值基于 Figma 设计源文件提取。圆角 Token 已在上方表格中完整列出。
