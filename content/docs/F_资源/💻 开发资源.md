---
title: 开发资源
description: 代码仓库、组件库、Token 包与开发工具
category: 资源
status: Draft
last_updated: 2026-01-31
---

# 💻 开发资源

本文档汇总 YAMI 设计系统相关的开发资源、代码仓库与技术文档。

---

## Design Token

### Token 仓库

| 资源 | 说明 |
|------|------|
| Token 源文件 | JSON 格式的 Token 定义 |
| 构建产物 | CSS / SCSS / Swift / Android 等平台文件 |

### Token 文档

| 文档 | 说明 |
|------|------|
| [[📝 Token命名规范]] | Token 命名规则与约定 |
| [[📁 Token文件结构]] | Token 文件组织与层级 |
| [[🔗 Token平台映射]] | 各平台的 Token 使用方式 |
| [[📜 Token更新日志]] | Token 版本变更历史 |

---

## 组件库

### Web 组件

| 技术栈 | 包名 | 状态 |
|--------|------|------|
| React | `@yami/react` | 规划中 |
| Vue | `@yami/vue` | 规划中 |

### 移动端组件

| 平台 | 包名 | 状态 |
|------|------|------|
| iOS (SwiftUI) | `YamiUI` | 规划中 |
| Android (Compose) | `yami-compose` | 规划中 |
| React Native | `@yami/rn` | 规划中 |

---

## 安装与使用

### Token 安装

#### NPM (Web)

```bash
npm install @yami/tokens
# 或
yarn add @yami/tokens
```

#### CSS 变量引入

```css
@import '@yami/tokens/css/variables.css';

.button {
  background-color: var(--color-ui-primary);
  padding: var(--space-200);
  border-radius: var(--radius-medium);
}
```

#### SCSS 变量引入

```scss
@import '@yami/tokens/scss/variables';

.button {
  background-color: $color-ui-primary;
  padding: $space-200;
  border-radius: $radius-medium;
}
```

#### Tailwind CSS 集成

```javascript
// tailwind.config.js
const yamiTokens = require('@yami/tokens/tailwind');

module.exports = {
  theme: {
    extend: {
      colors: yamiTokens.colors,
      spacing: yamiTokens.spacing,
      borderRadius: yamiTokens.borderRadius,
      boxShadow: yamiTokens.boxShadow,
    },
  },
};
```

---

## 开发工具

### 推荐工具链

| 工具 | 用途 |
|------|------|
| Style Dictionary | Token 构建与多平台导出 |
| Tokens Studio | Figma 与代码 Token 同步 |
| Storybook | 组件文档与预览 |
| Chromatic | 视觉回归测试 |

### VSCode 插件

| 插件 | 用途 |
|------|------|
| CSS Variable Autocomplete | CSS 变量自动补全 |
| Tailwind CSS IntelliSense | Tailwind 智能提示 |
| Color Highlight | 颜色值预览 |

---

## API 参考

### CSS 变量命名

```
--{类别}-{语义}[-{状态}]
```

**示例**：
- `--color-text-primary`
- `--color-ui-primary-hover`
- `--space-200`
- `--radius-medium`
- `--elevation-300`

### Token 类型定义 (TypeScript)

```typescript
interface ColorToken {
  value: string;
  type: 'color';
}

interface SpacingToken {
  value: string;
  type: 'spacing';
}

interface TypographyToken {
  fontSize: string;
  lineHeight: string;
  fontWeight: string;
}

interface ShadowToken {
  value: string;
  type: 'boxShadow';
}
```

---

## 常见问题

### Q: 如何在项目中覆盖 Token 值？

使用 CSS 变量的层叠特性：

```css
:root {
  --color-ui-primary: #E00000; /* 默认值 */
}

.theme-custom {
  --color-ui-primary: #0066CC; /* 覆盖值 */
}
```

### Q: 如何支持暗色模式？

深色模式主文字建议使用柔和白（如 `#E5E5E5` 或 `#DEDEDE`，等效于 Material 的 `rgba(255,255,255,0.87)`），避免纯白 `#FFFFFF` 造成刺眼。文字色应使用实色 hex，不推荐 rgba，以确保对比度可测、渲染清晰。

```css
@media (prefers-color-scheme: dark) {
  :root {
    --color-text-primary: #E5E5E5;
    --color-background-primary: #121212;
  }
}
```

或使用类名切换（如 `data-theme="dark"`）：

```css
[data-theme='dark'] {
  --color-text-primary: #E5E5E5;
  --color-background-primary: #121212;
}
```

### Q: Token 更新后如何同步？

1. 更新 Token 包版本
2. 运行构建脚本重新生成平台文件
3. 检查是否有破坏性变更（参见 [[📜 Token更新日志]]）

---

## 技术支持

- 提交 Issue 到设计系统仓库
- 加入技术讨论群组
- 联系前端基础设施团队

---
